name: Clojure CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: DeLaGuardo/setup-clojure@master
        with:
            cli: '1.10.1.469'
        - run: cd app && clojure -A:uberjar
